#include <iostream>
#include <string>
using namespace std;

int main(){
    string s = "aa";
    string p = "a";
    int len;
    // s.length() > p.length() ? len = s.length() : len = p.length();
    cout<< len<<endl;
    cout<<p[1];
    // for(int i = 0; i<len; i++){
    // }

}